{
  "name": "sbd-library-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "db:migrate": "node scripts/migrate.js",
    "db:seed": "node scripts/seed.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "next": "^14.2.29"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "dotenv": "^17.4.2"
  }
}
